# seedBank example configuration created by Jasper Poppe <jgpoppe@gmail.com>

# urls for getting the pxe boot files, ISOs and syslinux
#
# debian          -> main url of the Debian mirror to use
# debian_firmware -> url with the location of Debian firmware files
# debian_iso      -> url and name of the location to get Debian ISOs
# ubuntu          -> main url of the Ubuntu mirror to use
# syslinux        -> tar archive of the syslinux release to use (will be 
#                    used used by the 'seedbank manage --syslinux' command
urls:
  debian: 'http://ftp.debian.org'
  debian_firmware: 'http://cdimage.debian.org/cdimage/unofficial/non-free/firmware/${release}/current/firmware.tar.gz'
  debian_iso: 'http://cdimage.debian.org/debian-cd/current/${architecture}/iso-cd/debian-${version}-${architecture}-businesscard.iso'
  ubuntu: 'http://archive.ubuntu.com'
  ubuntu_iso: 'http://mirrors.kernel.org/ubuntu-releases/${release}/ubuntu-${release}-${version}-${architecture}.iso'
  syslinux: 'http://www.kernel.org/pub/linux/utils/boot/syslinux/4.xx/syslinux-4.05.tar.gz'
